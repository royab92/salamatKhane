<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rb.salamatkhane">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".AdminRegisterActivity" />
        <activity
            android:name=".BuyActivity"
            android:label="@string/title_activity_buy"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SellActivity"
            android:label="@string/title_activity_sell"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SellRequestActivity"
            android:label=""
            android:theme="@style/AppTheme" />
        <activity android:name=".BuyRequestActivity" />
        <activity android:name=".FilterActivity" />
        <activity android:name=".WaitingActivity" />
        <activity
            android:name=".MyBuyActivity"
            android:label="@string/title_activity_my_buy"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MySellActivity"
            android:label="@string/title_activity_my_sell"
            android:theme="@style/AppTheme" />
        <activity android:name=".RemoveActivity" />
        <activity android:name=".ReportActivity" />
        <activity android:name=".EditInfoActivity"></activity>
    </application>

</manifest>